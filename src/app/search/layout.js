export const metadata = {
  title: "Search Products — O-Jain",
  description: "Search for your favourite O-Jain Jain & Satvik premix products. Find by name, flavour, or category.",
};

export default function Layout({ children }) {
  return children;
}
