export const metadata = {
  title: "Product Category — O-Jain",
  description: "Explore O-Jain products in this category. Pure Jain & Satvik premix — no onion, no garlic, no preservatives.",
};

export default function Layout({ children }) {
  return children;
}
