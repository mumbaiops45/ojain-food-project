export const metadata = {
  title: "Vendor Login — O-Jain",
  description: "Login to your O-Jain vendor account to manage your products, orders, and store profile.",
};

export default function Layout({ children }) {
  return children;
}
